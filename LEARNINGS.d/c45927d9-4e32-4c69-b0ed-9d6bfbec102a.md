---
uuid: c45927d9-4e32-4c69-b0ed-9d6bfbec102a
kind: session
date: 2026-07-27
captured: 2026-07-27
title: Repurposed the repo from TikTok DNU-CPI to re-architecting the Creative Performance Analyzer GAS app
model: claude-opus-5[1m]
---

## 2026-07-27 — c45927d9-4e32-4c69-b0ed-9d6bfbec102a

- **Finding:** `clasp clone-script <scriptId>` reads any Apps Script project the logged-in user can
  open — that is how the target app (`1WMWxrw4…`, 2901 + 3066 lines) got into the repo, no editor
  copy-paste. But **Drive file content is a different door**: the app's runtime skill lives in two
  Drive files behind `SKILL_FILE_ID`/`KB_FILE_ID`, they are not link-public (403 unauthenticated),
  and scripting local `auth/` tokens against the Drive API is refused by the permission classifier as
  credential harvesting. The usable substitute was `MCO_SYSTEM_PROMPT_FALLBACK` in `Code.js` — the
  app's own in-source copy of that skill, 11,186 chars, complete.
- **Finding:** the app's real architecture is worth naming before touching it — `runSQLParallel`
  (slugs created sequentially because each needs auth, then all `/run/json` fired through
  `UrlFetchApp.fetchAll()`) across two batches of 13 queries, and a **dated** PDT
  (`lr_rbd0d1785124923808_cstudio__creative_format` today) that must always be resolved via
  `SHOW TABLES FROM looker LIKE '%cstudio__creative_format%'`, never hardcoded.
- **Shift:** repo scope narrowed to four things — vendored GAS source, Trino access layer for
  *verifying* SQL before it lands in `Code.js`, the MCO skill, and the learn/commit loop. The
  inherited TikTok DNU-CPI skill + dashboard + Trino cuts are deleted (recoverable from `02f44c2`);
  `build_creative_sql` went with them. Deps down to `mcp` + `looker-sdk`.
- **Shift:** the standing hazard in this codebase is **duplicated knowledge, not bugs** — MCO rules
  exist in 4 places (Drive, in-source fallback, `localMcoDiagnosis` in `Dashboard.html`, this repo's
  SKILL.md) and the inventory-group mapping in 3. Every change must land in all copies or none; that
  is now the first thing `.claude/gas-push-checklist.txt` says.
- **Watch:** three unresolved. (1) `git push`/`pull` are blocked by the permission classifier in this
  environment and `main` has no upstream — commits `5848021`/`adabdbd`/`5e891dd` sit local until the
  user pushes or grants a rule. (2) `skills/mco-creative-explainer/SKILL.md` is **unverified against
  Drive** — Drive wins at runtime, so if it drifted this file is silently stale. (3) `Code.js:40`
  hardcodes the Looker `CLIENT_ID` (now committed) and both AI calls pin
  `claude-sonnet-4-20250514` — both queued, neither fixed.
