[project]
name = "creative-performance-analyser"
version = "0.1.0"
description = "Workspace for re-architecting the Creative Performance Analyzer Apps Script app — Looker/Trino access layer + MCP server"
requires-python = ">=3.14"
# Deliberately minimal: just what creative_mcp.py needs. Add extras ad hoc with
#   uv pip install <pkg> --python ./python-virtual-environment/bin/python3
dependencies = [
    "mcp>=1.27.1",
    "looker-sdk>=26.6.1",
]
