Before you finish this session: if it involved substantive analysis, decisions, or corrections worth remembering, append ONE distilled entry to YOUR SESSION'S OWN learnings file (the exact path is given below) — at the top, newest-first, dated (YYYY-MM-DD), using the Finding / Shift / Watch tags. Capture the *wisdom* — what was learned, decided, or should be watched — in a few tight lines, NOT a transcript. If a new learning supersedes an older entry, annotate the old one rather than deleting it. If nothing substantive happened this session, do NOT add an entry — just finish. If you have already added an entry to your session file this session, UPDATE that same entry rather than creating a duplicate. When creating your session file for the first time, begin it with the YAML frontmatter block (uuid, kind: session, date, captured, title, model) per LEARNINGS.d/README.md so it is self-describing and shows up in the generated INDEX. Do NOT edit the archive (LEARNINGS.d/ARCHIVE.md) or any other session's file — writing only to your own file is what prevents the concurrent-session write race (see LEARNINGS.d/README.md). Do this, then stop.