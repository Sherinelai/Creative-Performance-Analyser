Also, before you finish: run `git status` and check whether you've crossed a commit checkpoint since your last commit (per CLAUDE.md's "Incremental Commits — Multi-Session Safety" section) — a working state, a completed sub-task, a verified file. If so, run the commit cycle now: `git add <specific files>` (never -A or .), a small single-purpose commit, `git pull --rebase origin main`, then push if clean (follow the Conflict Protocol in that section if the rebase hits conflicts — never force-push, never blind --ours/--theirs). If `git status` shows unrelated untracked/modified files outside your current checkpoint's scope, don't fold them in and don't ignore them either — ask the user what to do with them. If nothing is checkpoint-worthy right now, don't commit — just finish.
